import ThemeProvider from 'providers/Theme';

const App = () => {
    return (
        <ThemeProvider>
            <div className="App">Tow Truck App</div>
        </ThemeProvider>
    );
};

export default App;
