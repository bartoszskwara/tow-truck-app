import { Box } from '@mui/material';
import StationsList from './StationsList';

const Stations = () => {
    return (
        <Box>
            <StationsList />
        </Box>
    );
};

export default Stations;
